name: Deploy Apigee Proxy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Modules/apigee/proxies

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - name: Build and Deploy Apigee Proxy
        run: |
          mvn clean install -Peval \
            -Dorg=internship-439613 \
            -Denv=eval \
            -Dbearer="ya29.a0ARGnu0aW2ercLsz7nYcbCOVJHrMo0oCO3RhAqMI6a6M1hcyxKgPbMXoOHP8sGWsqPNbo73N3Gjo-gIfTr_hYrsg3FMk__Cym5cH_pSFE_RQ9f52ftzJRItLy-RiSa1vM84os-e4KVLtgy5Z6yWDPXy3RPWrl3Jq-qkXr_q_Ne8KffnUuB5G5vMpzLZI4Qq6GcJR3TcBH89UJaCgYKAVsSARESFQHGX2Mix-3PuBL_8SeN3b-4UnDFKw0211" \
            -Dapigee.options=override
